import * as actions from '<%= name %>/actions'
import * as types from '<%= name %>/types'
import * as constants from '<%= name %>/constants'
import * as selectors from '<%= name %>/selectors'
import * as components from '<%= name %>/components'
import * as epics from '<%= name %>/epics'
import reducer from '<%= name %>/reducer'

export default { actions, types, selectors, reducer, epics, constants, components }
